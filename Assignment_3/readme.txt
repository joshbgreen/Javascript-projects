Add an image
Add a button
Create a function which allows the image to move from left to right 
via clicking the button

ext:
add multiple images
create a function that moves the images at random distances over a set 
interval until the first reaches the finish line
output the winner
